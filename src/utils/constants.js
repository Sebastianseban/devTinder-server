// Fields safe to expose in API responses
export const USER_SAFE_DATA =
  "firstName lastName photoUrl age gender bio experienceLevel skills isPremium";

// Pagination defaults
export const DEFAULT_PAGE_SIZE = 10;
export const MAX_PAGE_SIZE = 50;
